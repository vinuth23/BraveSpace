name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  cloud_firestore: ^5.5.0
  http: ^1.3.0
  flutter:
    sdk: flutter

  # Updated Firebase dependencies
  firebase_core: ^3.9.0  # Latest stable version
  firebase_auth: ^5.3.4  # Latest stable version that works with core 2.24.2

  cupertino_icons: ^1.0.8
  # Remove the flutter_svg dependency if not needed
  # flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/  # Keep the PNG reference
