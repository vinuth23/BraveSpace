name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter

  # Firebase dependencies (add these for Firebase)
  firebase_core: ^2.10.0 # This is needed to initialize Firebase
  firebase_auth: ^4.0.0  # Add other Firebase dependencies as per your app's requirements

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
